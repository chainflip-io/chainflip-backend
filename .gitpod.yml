image:
  file: .gitpod.Dockerfile
tasks:
  - prebuild: cargo check --release

github:
  prebuilds:
    branches: true
    pullRequestsFromForks: true
    addBadge: true
    addComment: true
    pullRequests: true
