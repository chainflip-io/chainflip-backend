
image:
  file: .gitpod.Dockerfile
tasks:
  - init: cargo cf-check-all

github:
  prebuilds:
    branches: true
    addBadge: true
    addComment: false
    pullRequests: true
    addCheck: false

vscode:
  extensions:
    - matklad.rust-analyzer
    - serayuzgur.crates
    - tamasfe.even-better-toml
