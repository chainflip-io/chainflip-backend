image:
  file: .gitpod.Dockerfile
tasks:
  - prebuild: SCCACHE_DIR=/cache cargo check --release

github:
  prebuilds:
    branches: true
    pullRequestsFromForks: true
    addBadge: true
    addComment: true
    pullRequests: true
