
image:
  file: .gitpod.Dockerfile
tasks:
  - prebuild: cargo check

github:
  prebuilds:
    branches: true
    pullRequestsFromForks: true
    addBadge: true
    addComment: false
    pullRequests: true

vscode:
  extensions:
    - matklad.rust-analyzer
    - serayuzgur.crates
    - tamasfe.even-better-toml
