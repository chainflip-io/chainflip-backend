[package]
authors = ['Chainflip Team <https://github.com/chainflip-io>']
description = 'FRAME pallet template for distributing FLIP rewards.'
edition = '2018'
homepage = 'https://chainflip.io'
license = '<TODO>'
name = 'pallet-cf-rewards'
repository = 'https://github.com/chainflip-io/chainflip-backend'
version = '0.1.0'

[package.metadata.docs.rs]
targets = ['x86_64-unknown-linux-gnu']

[dependencies]
codec = { default-features = false, features = ['derive'], package = 'parity-scale-codec', version = '2.0.0' }
frame-system = { default-features = false, version = '3.0.0' }
frame-support = { default-features = false, version = '3.0.0' }
pallet-cf-flip = { path = '../cf-flip', default-features = false }
pallet-cf-validator = { path = '../cf-validator', default-features = false }
cf-traits = { path = '../../traits', default-features = false }
sp-std = { default-features = false, version = '3.0.0' }
sp-runtime = { default-features = false, version = '3.0.0' }

[dev-dependencies]
serde = { version = "1.0.119" }
sp-core = { default-features = false, version = '3.0.0' }
sp-io = { default-features = false, version = '3.0.0' }

[features]
default = ['std']
std = [
	'cf-traits/std',
	'codec/std',
	'frame-support/std',
	'frame-system/std',
	'pallet-cf-flip/std',
	'pallet-cf-validator/std',
	'sp-runtime/std',
	'sp-std/std',
]