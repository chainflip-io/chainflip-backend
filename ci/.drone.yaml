kind: pipeline
type: docker
name: chainflip-backend

steps:
  - name: build-test-rust
    image: ghcr.io/chainflip-io/chainflip-infrastructure/build/state-chain-base:latest
    environment:
      SCCACHE_DIR: /cache
      SCCACHE_LOG: debug
      WASM_BUILD_TOOLCHAIN: nightly
    commands:
      - chmod +x ci/scripts/check.sh
      - ./ci/scripts/check.sh
    volumes:
      - name: cache
        path: /cache
trigger:
  event:
    - push

volumes:
  - name: cache
    host:
      path: /mnt/chainflip-build-runner/sccache
node:
  builder: "true"
