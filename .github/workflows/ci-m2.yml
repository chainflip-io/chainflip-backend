name: Build on M2

on: push

concurrency:
  group: ${{ github.ref }}-build-m2
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/_21_build_m2.yml
    secrets: inherit
    with:
      profile: "release"
