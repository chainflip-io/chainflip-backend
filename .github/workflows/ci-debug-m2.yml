on:
  push

jobs:
  build-m2:
    uses: ./.github/workflows/_21_build_m2.yml
    secrets: inherit
    with:
      profile: "release"
  build-m2-production:
    uses: ./.github/workflows/_21_build_m2.yml
    secrets: inherit
    with:
      profile: "production"
      binary-subdir: "production"
      ci-runner: "rust-builder-m2-no-cache"
