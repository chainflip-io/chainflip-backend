name: Release Chainflip
on:
  push:
    branches:
      - 'release/*'
      - chore/ci-refactor
    tags:
      - 'v*'
jobs:
  check:
    uses: ./.github/workflows/_check.yml@chore/ci-refactor
#  build-and-test:
#    uses: .github/workflow/reuseable/build-and.test.yaml
#  release-sisyphos:
#  release-perseverance: