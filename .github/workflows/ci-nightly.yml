name: Nightly CI 😴
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

env:
  FORCE_COLOR: 1

jobs:
  coverage:
    uses: ./.github/workflows/_11_coverage.yml
    with:
      test_features: runtime-benchmarks,slow-tests
    secrets: inherit