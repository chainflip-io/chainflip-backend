# Note: If you just want to save typing command commands, you can install tab completions for most shells. Type
# `rustup completions --help` to find out how to set this up.

[alias]
cf-test =     "test --lib --features runtime-benchmarks,try-runtime,runtime-integration-tests,integration-test,cli"
cf-test-ci =  "test --lib --features runtime-benchmarks"
cf-test-cfe = "test --lib --package chainflip-engine"

cf-clippy =    "clippy           --all-targets --features runtime-benchmarks,try-runtime,runtime-integration-tests,integration-test,cli -- -D warnings"
cf-clippy-ci = "clippy --release --all-targets --features runtime-benchmarks,try-runtime,runtime-integration-tests,integration-test,cli -- -D warnings"

cf-build =    "build --features runtime-benchmarks"
cf-build-ci = "build --release --features runtime-benchmarks"

# Check for feature inconsistencies.
check-sc-features = '''
tree --no-default-features --depth 1 --edges=features,normal
    -p state-chain-*
    -p pallet-cf-*
    -p cf-*
'''
